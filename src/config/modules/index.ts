import userConfig from './user';
import productConfig from './product';
import orderConfig from './order';

export const moduleConfigs = [
  userConfig,
  productConfig,
  orderConfig,
];

export { userConfig, productConfig, orderConfig };
